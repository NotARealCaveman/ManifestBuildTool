#include "EXPERIMENTAL_MESSAGING_STRUCTURES.h"

using namespace Manifest_Experimental;

void ObersvableFileSystem::OnLoad(const int& loadEvent)
{
	DLOG(31, "File system recv'd load event: " << loadEvent);	
}